<UserControl
    x:Class="CRust.FileExplorer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CRust"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="300">

    <TreeView Background="{ThemeResource SystemControlBackgroundListLowBrush}">
        <TreeView.RootNodes>
            <TreeViewNode Content="File Explorer" Typography.StylisticSet1="True"/>
            <TreeViewNode Content="src" IsExpanded="True">
                <TreeViewNode.Children>
                    <TreeViewNode Content="main.rs"/>
                </TreeViewNode.Children>
            </TreeViewNode>
            <TreeViewNode Content="Cargo.lock"/>
            <TreeViewNode Content="Cargo.toml"/>
        </TreeView.RootNodes>
    </TreeView>
</UserControl>
